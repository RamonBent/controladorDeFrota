spring.application.name=controladorFrota

# Configuração do banco de dados MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/bd_controladorFrota?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Configuração do Hibernate (JPA)
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
spring.jpa.hibernate.ddl-auto=update

#spring.jpa.hibernate.ddl-auto=create-drop


# Exibir queries SQL no console
spring.jpa.show-sql=true  

#Desativando o security
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.security.servlet.SecurityAutoConfiguration

#Para ativar o security
#spring.security.user.name=ra
#spring.security.user.password=ra
